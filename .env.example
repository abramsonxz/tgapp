# ===========================================
# QuestBoard — Environment Variables
# ===========================================

# DATABASE
# Railway: create a PostgreSQL service, then set:
DATABASE_URL="postgresql://user:password@host:5432/dbname"
# Railway auto-sets this via reference: ${Postgres.DATABASE_URL}

# The app runs on port 3000 (Railway auto-detects)
PORT=3000
