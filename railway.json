{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "prisma generate && prisma db push && node server.js",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}
